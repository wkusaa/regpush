name: regpush
description: Push an already-built local Docker image through a chunk-aware OCI registry upload protocol
author: wkusaa

inputs:
  image:
    description: Fully qualified local image reference, including registry and repository
    required: true
  username:
    description: Registry username
    required: true
  password:
    description: Registry password
    required: true
  insecure-http:
    description: Allow plain HTTP instead of HTTPS
    required: false
    default: "false"
  cleanup:
    description: Remove temporary files; set false to retain and reuse a validated image-ID cache
    required: false
    default: "true"

runs:
  using: node24
  main: dist/index.js

branding:
  icon: upload-cloud
  color: orange
